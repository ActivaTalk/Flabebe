export enum LinksES {
  HOME = "/",
  FOREX = "/forex",
  PRODUCTS = "/productos",
  CONTACT = "/contacto",
  LEGALS = "/legales",
}

export enum LinksEn {
  HOME = "/",
  FOREX = "/forex",
  PRODUCTS = "/products",
  CONTACT = "/contact",
  LEGALS = "/legals",
}
